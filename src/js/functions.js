// import { Popup } from "./libs/popup";

// console.log('functions is active')

// export const initPopups = (logging = false, init = true) => new Popup({ logging: logging, init: init });

// console.log(initPopups(false))